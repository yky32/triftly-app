# Local-only secrets (gitignored). Copy to env/.env.local and fill values.
# Source of truth for CI: GitHub repository secrets.
# Get values from: https://github.com/yky32/triftly-app/settings/secrets/actions
# (GitHub does not let you export secret values — copy from Supabase / Google Cloud once.)

GOOGLE_MAPS_API_KEY=
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=
