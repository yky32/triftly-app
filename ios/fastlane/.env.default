# Fastlane defaults (no secrets). Use ios/fastlane/.env for local credentials.
# See .env.default.example
# GitHub Actions uses repository secrets — never put credentials in this file.
# Supabase: set SUPABASE_URL + SUPABASE_PUBLISHABLE_KEY as repo secrets for TestFlight builds.
